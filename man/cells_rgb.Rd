% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colour.R
\name{cells_rgb}
\alias{cells_rgb}
\title{Build per-cell packed-integer RGB colours from three numeric channels}
\usage{
cells_rgb(r, g, b, range = NULL)
}
\arguments{
\item{r, g, b}{Numeric vectors of identical length supplying the red,
green and blue channels.}

\item{range}{Optional length-2 numeric \code{c(min, max)} shared across all
three channels. \code{NULL} (default) rescales each channel independently.}
}
\value{
An integer vector of packed RGB values, the same length as
the inputs.
}
\description{
Convenience helper for the common workflow of rescaling three numeric
vectors (e.g. spectral bands) to \code{0:255} and combining them into the
packed \verb{(R << 16) | (G << 8) | B} integer form expected by
\code{\link[=a5_view]{a5_view()}} with \code{fill_identity = TRUE}.
}
\details{
Each channel is rescaled to \code{0:255} independently using its own
min/max, unless \code{range} is supplied, in which case the same bounds
are applied to all three channels (useful when the bands are
radiometrically comparable). Values outside \code{range} are clipped.
\code{NA} inputs propagate to \code{NA} in the output. Constant channels (or
channels that are entirely \code{NA} under per-channel scaling) contribute
zero.
}
\examples{
n <- 5
df <- data.frame(b1 = runif(n), b2 = runif(n), b3 = runif(n))
df$rgb <- cells_rgb(df$b1, df$b2, df$b3)

}
